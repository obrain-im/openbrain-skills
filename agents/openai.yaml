interface:
  display_name: "Open Brain"
  short_description: "Manage Open Brain agents, daemon, and services"
  default_prompt: "Use $open-brain to initialize this workspace, register or reload its agent runtime, and expose its REST services through Open Brain."
